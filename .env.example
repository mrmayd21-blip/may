# Environment variables for Docker Compose and production deployment
# Copy this to .env and update with your own values

# Flask
FLASK_ENV=production
SECRET_KEY=your-secret-key-here-min-32-chars
ADMIN_PASS=your-secure-admin-password

# Optional: SMTP for password reset emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@example.com

# Optional: Database URL (for PostgreSQL instead of SQLite)
# DATABASE_URL=postgresql://user:pass@localhost/messaging_db
